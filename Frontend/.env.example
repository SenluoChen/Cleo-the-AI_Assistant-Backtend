# API endpoint consumed by the renderer when the user submits a request
VITE_ANALYZE_URL=https://example.com/analyze

# Enable native screen capture features (set to "true" to allow captures)
SMART_ASSISTANT_ENABLE_SCREEN_CAPTURE=false
